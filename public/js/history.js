let historyItems = [];
let deleteDraftId = '';
const esc = (value) => String(value ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/"/g,'&quot;');
function formatDate(value){if(!value)return 'Date unavailable';return new Intl.DateTimeFormat('en-GB',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value));}
function renderHistory(){const filter=document.getElementById('history-filter').value;const list=historyItems.filter(item=>filter==='all'||item.status===filter);const root=document.getElementById('history-list');if(!list.length){root.innerHTML='<div class="empty-setup"><strong>No matching history</strong><span>Change the filter or start a quarterly update.</span></div>';return}root.innerHTML=list.map(item=>`<article class="history-event ${item.status}"><span class="history-marker"></span><div><time>${esc(formatDate(item.createdAt))}</time><h3>${esc(item.title)}</h3><p>${esc(item.detail)}</p><span class="history-status">${esc(item.statusLabel)}</span></div><div class="history-event-actions">${item.kind==='draft'?`<a class="btn btn-primary btn-sm" href="/app?draftId=${encodeURIComponent(item.id)}">Open draft</a><button class="btn btn-ghost btn-sm" data-delete-draft="${esc(item.id)}">Delete</button>`:`<a class="btn btn-ghost btn-sm" href="/api/receipts/${encodeURIComponent(item.id)}">View receipt</a><a class="btn btn-ghost btn-sm" href="/api/receipts/${encodeURIComponent(item.id)}?download=1">Download</a>`}</div></article>`).join('');root.querySelectorAll('[data-delete-draft]').forEach(button=>button.onclick=()=>{deleteDraftId=button.dataset.deleteDraft;document.getElementById('delete-draft-dialog').showModal()});}
async function load(){const me=await fetch('/api/auth/me').then(r=>r.json());if(!me.user){document.getElementById('gate').hidden=false;document.getElementById('gate').innerHTML='<a href="/signin?next=/history">Sign in</a> to view your submission history.';return}const [draftResponse,submissionResponse,statusResponse]=await Promise.all([fetch('/api/drafts'),fetch('/api/me/submissions'),fetch('/api/status')]);const drafts=(await draftResponse.json()).drafts||[];const submissions=(await submissionResponse.json()).submissions||[];const status=await statusResponse.json();document.getElementById('history-mode').textContent=status.hmrcMode==='live'?'Production HMRC configured':status.oauthMock===false?'HMRC sandbox configured':'Preview mode';historyItems=[...drafts.map(d=>({kind:'draft',id:d.id,createdAt:d.createdAt,title:d.filename||'Spreadsheet draft',detail:'Saved draft · not submitted',status:'draft',statusLabel:'Draft'})),...submissions.map(s=>({kind:'submission',id:s.id,createdAt:s.createdAt,title:s.ok?'Submission attempt completed':'Submission attempt failed',detail:`${s.mode||'unknown'} mode · receipt ${s.id.slice(0,8)}`,status:s.ok?'ok':'failed',statusLabel:s.ok?(s.mode==='double'?'Preview completed':'Submitted'):'Failed'}))].sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt)));document.getElementById('history-total').textContent=historyItems.length;document.getElementById('history-success').textContent=submissions.filter(s=>s.ok).length;document.getElementById('history-action').textContent=submissions.filter(s=>!s.ok).length;renderHistory();}
document.getElementById('history-filter').addEventListener('change',renderHistory);document.getElementById('delete-draft-dialog').addEventListener('close',async event=>{if(event.target.returnValue!=='confirm'||!deleteDraftId)return;const response=await fetch(`/api/drafts/${encodeURIComponent(deleteDraftId)}`,{method:'DELETE'});deleteDraftId='';if(!response.ok){document.getElementById('gate').hidden=false;document.getElementById('gate').textContent='The draft could not be deleted.';return}await load()});document.getElementById('cc-menu-toggle').addEventListener('click',()=>{const side=document.getElementById('cc-sidebar');const open=side.classList.toggle('open');document.getElementById('cc-menu-toggle').setAttribute('aria-expanded',String(open))});load();
